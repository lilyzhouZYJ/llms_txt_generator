# Copy this file to `.env` and fill in your key (`.env` is gitignored).
# For local runs: `export OPENAI_API_KEY=...` or use a dotenv loader if you add one.

OPENAI_API_KEY=
# Optional: defaults to gpt-4o-mini in code
# OPENAI_MODEL=gpt-4o-mini
